import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Partner With a Sports Management Company in Mumbai",
  description: "Become a vendor, sponsor, or collaborator with Force Sports United, a leading sports management company in Mumbai.",
  keywords: [
    "partner with sports management company Mumbai",
    "sports sponsorship Mumbai",
    "sports event vendor Mumbai",
  ],
  alternates: { canonical: "/partner" },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
